# Kapi Backend — Python 3.14 / Windows compatible
# Uses >= constraints; exact versions pinned only where necessary.
# Install torch separately: pip install torch --index-url https://download.pytorch.org/whl/cpu

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Data processing (pandas 3.x has Python 3.14 wheels; 2.x does not on Windows)
pandas>=3.0.0
numpy>=2.0.0

# AI providers
anthropic>=0.25.0
openai>=1.30.0

# RAG / embeddings (install torch first via PyTorch CPU index)
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0

# Browser auth
playwright>=1.44.0

# Reports + utils
jinja2>=3.1.0
httpx>=0.27.0
aiofiles>=23.2.0
python-dateutil>=2.9.0

# Auth (Clerk JWT verification)
PyJWT[crypto]>=2.8.0

# Billing (Stripe)
stripe>=9.0.0

# Dev / eval
pytest>=8.0.0
pytest-asyncio>=0.23.0
